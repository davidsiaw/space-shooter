#ifndef DEFINITIONS_H
#define DEFINITIONS_H

#include <boost/shared_ptr.hpp>

// Marking definitions
// These defines are just to make the code more concise, and are not enforced

// A define to make it easy to see the class is intended to be an interface
#define INTERFACE class

// define to mark that we are implementing an interface
#define IMPLEMENTS public

#endif // DEFINITIONS_H
